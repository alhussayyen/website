# ملاحظات للمطوّر

هذه الملاحظات كانت داخل `index.html` كتعليقات مؤقتة، تم نقلها هنا بعد تنظيف الملف.

## 1. قبل النشر (SEO)

استبدل النطاق `https://siirah.sa/` بالنطاق الفعلي للموقع في جميع الوسوم التالية داخل `<head>`:

- `<link rel="canonical">`
- وسوم `og:url`, `og:image`
- وسوم `twitter:image`
- بيانات JSON-LD (`@id`, `url`, `logo.url`, `image`)

وأيضًا في `sitemap.xml` و`robots.txt` إذا/عند إضافتهما.

## 2. قسم "الإنتاج المرئي"

بطاقات `.video-slot` الأربع (`v1`–`v4`) هي عناصر نائبة مؤقتة (placeholders) بانتظار مقاطع الفيديو الفعلية. كل بطاقة تحمل `data-slot` لتحديد ترتيبها. عند استلام المقاطع، استبدل كل `div.video-slot` بعنصر `<video>` فعلي (مع الحفاظ على نفس الـ class وborder-radius من `style.css`).

## 3. الملف التعريفي (PDF) — مطلوب قبل الإطلاق

زر "تحميل الملف التعريفي (PDF)" في قسم `#profile` يشير إلى:

```
assets/docs/company-profile.pdf
```

هذا الملف غير موجود حاليًا (لم يتوفر تصميم/محتوى رسمي للملف التعريفي). ضع ملف PDF فعلي بهذا الاسم بالضبط في المسار `assets/docs/` وسيعمل الزر تلقائيًا دون أي تعديل على الكود. **هذا آخر عنصر يمنع الإطلاق التجاري الكامل** — كل شيء آخر في الموقع جاهز.

## 4. صورة og:image

وسم `og:image` يشير إلى `https://siirah.sa/og-image.jpg` (رابط خارجي على النطاق النهائي). تأكد من رفع صورة 1200×630 بهذا الاسم إلى جذر الموقع عند النشر، وإلا ستظهر معاينة فارغة عند مشاركة الرابط في واتساب/تويتر/إلخ.
