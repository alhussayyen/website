# ملاحظات للمطوّر

هذه الملاحظات كانت داخل `index.html` كتعليقات مؤقتة، تم نقلها هنا بعد تنظيف الملف. آخر تحديث: مراجعة Production Readiness.

## عناصر ما زالت مفتوحة قبل الإطلاق

### 1. النطاق الفعلي (SEO) — لم يُحدَّد بعد

كل الوسوم التالية داخل `<head>`، وكذلك `site.webmanifest`، ما زالت تشير إلى نطاق مؤقت (`https://siirah.sa/`) لأن النطاق النهائي غير معروف بعد:

- `<link rel="canonical">`
- وسوم `og:url`, `og:image`
- وسوم `twitter:image`
- بيانات JSON-LD (`@id`, `url`, `logo.url`, `image`)

استبدله بالنطاق الفعلي في جميع هذه المواضع فور تحديده، وأضف `sitemap.xml`/`robots.txt` عند النشر.

### 2. صورة og:image غير موجودة بعد

وسم `og:image` يشير إلى `https://siirah.sa/og-image.jpg` — صورة مشاركة اجتماعية (1200×630) لم يتم تصميمها بعد. بدونها ستظهر معاينة فارغة عند مشاركة الرابط في واتساب/تويتر/إلخ. تحتاج تصميم مخصص (ليست مجرد تصغير للشعار)، لذا لم تُنشأ تلقائيًا ضمن مراجعة الجاهزية للإنتاج — ضعها في جذر النشر بهذا الاسم بالضبط عند توفرها.

## عناصر تم إنجازها (سابقًا مفتوحة، أُغلقت الآن)

- **الملف التعريفي (PDF):** `assets/docs/company-profile.pdf` موجود ويعمل زر التحميل في قسم `#profile` بشكل صحيح.
- **قسم "الإنتاج المرئي":** الفيديوهات الفعلية الأربعة (`assets/video/video-01.mp4`…`04.mp4` + صور الغلاف الخاصة بها) مرفوعة ومربوطة بالكود؛ لم تعد بطاقات placeholder.
- **Favicons / site.webmanifest:** كانت الملفات مُشارًا إليها في `<head>` لكنها غير موجودة فعليًا (كل طلب صفحة كان يُرجع 404 لها). تم توليدها من شعار العلامة (`assets/images/logo-mark.png`) وإضافتها في جذر المشروع: `favicon.ico`, `favicon-16x16.png`, `favicon-32x32.png`, `favicon-48x48.png`, `apple-touch-icon.png`, `android-chrome-192x192.png`, `android-chrome-512x512.png`, `site.webmanifest`.
